<div class=container>
  <h1 class="form-title">ログイン</h1>
  <div class="row">
    <div class="col-md6 col-md-offset-3 form-wrapper">
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class=form-group>
          <%= f.label :email, "メールアドレス" %>
          <%= f.email_field :email, autofocus: "true", autocomplete: "email", class: "form-control", placeholder: "xxx@email.com" %>
        </div>
        <div class="form-group">
          <%= f.label :password, "パスワード" %>
          <%= f.password_field :password, autocomplete: "current-password", class: "form-control" %>
        </div>
        <% if devise_mapping.rememberable? %>
          <div class="field">
            <%= f.check_box :remember_me %>
            <%= f.label :remember_me %>
          </div>
        <% end %>
        <%= f.submit "ログイン", class: "btn btn-success btn-block form-submit-btn" %>
      <% end %>
    </div>
  </div>
</div>
