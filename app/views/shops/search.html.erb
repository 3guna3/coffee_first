<h1>検索結果</h1>
<table>
  <thead>
  </thead>
  <tbody>
    <% @results.each do |shop| %>
      <tr>
        <td><%= shop.name %></td>
        <td><%= shop.prefecture %></td>
        <td><%= shop.address %></td>
        <td><%= link_to '詳細', shop %></td>
      </tr>
    <% end %>
  </tbody>
</table>
